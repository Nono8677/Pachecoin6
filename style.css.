* { box-sizing: border-box; }

body {
    background-color: #000000;
    color: #ffffff;
    font-family: -apple-system, sans-serif;
    margin: 0;
    padding: 0;
}

#auth-screen {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 20px;
    background-color: #000000;
}

#auth-screen .logo-pache {
    font-size: 3rem;
    margin-bottom: 30px;
}

.auth-card {
    background: #0d0d0d;
    border-radius: 16px;
    padding: 30px 24px;
    width: 100%;
    max-width: 360px;
    border: 1px solid #1a1a1a;
}

.auth-tabs {
    display: flex;
    margin-bottom: 24px;
    border-bottom: 1px solid #1a1a1a;
}

.auth-tab {
    flex: 1;
    background: none;
    border: none;
    color: #555;
    padding: 10px;
    font-size: 0.95rem;
    cursor: pointer;
}

.auth-tab.active {
    color: #ff8c00;
    border-bottom: 2px solid #ff8c00;
}

.auth-card input {
    width: 100%;
    background: #111111;
    border: 1px solid #222;
    color: white;
    padding: 13px 14px;
    border-radius: 8px;
    font-size: 1rem;
    margin-bottom: 12px;
    outline: none;
}

.auth-card input:focus { border-color: #ff8c00; }

.auth-btn {
    width: 100%;
    background: #ff8c00;
    color: #000;
    border: none;
    padding: 14px;
    border-radius: 8px;
    font-weight: 800;
    font-size: 1rem;
    cursor: pointer;
    margin-top: 4px;
}

.auth-btn:active { background: #e07b00; }

#auth-message {
    text-align: center;
    font-size: 0.85rem;
    margin-top: 14px;
    min-height: 20px;
}

#main-app { padding: 15px; }

header { text-align: center; margin-bottom: 25px; }

.logo-pache {
    color: #ff8c00;
    background-color: #000000;
    font-size: 2.8rem;
    margin: 0;
    font-weight: 900;
    letter-spacing: 2px;
    padding: 4px 0;
}

#market-timer {
    color: #ff8c00;
    font-weight: bold;
    margin-top: 8px;
    font-size: 1.2rem;
    font-family: monospace;
}

#last-update { font-size: 0.75rem; opacity: 0.5; margin: 4px 0; }

#logout-btn {
    background: none;
    border: 1px solid #1a1a1a;
    color: #555;
    padding: 6px 12px;
    border-radius: 6px;
    font-size: 0.75rem;
    cursor: pointer;
    margin-top: 8px;
}

.tabs {
    display: flex;
    justify-content: center;
    border-bottom: 1px solid #1a1a1a;
    margin-bottom: 20px;
}

.nav-tab {
    background: none;
    border: none;
    color: #555;
    padding: 12px 20px;
    font-size: 1rem;
    cursor: pointer;
}

.nav-tab.active { color: #ff8c00; border-bottom: 2px solid #ff8c00; }

.tab-content { display: none; }
.tab-content.active { display: block; }

.controls { display: flex; justify-content: space-between; margin-bottom: 15px; }

#refresh-btn {
    background: #111111;
    color: white;
    border: none;
    padding: 10px 18px;
    border-radius: 6px;
    font-size: 1.1rem;
    cursor: pointer;
}

select {
    background: #111111;
    color: white;
    border: none;
    padding: 10px;
    border-radius: 6px;
}

.crypto-card {
    background: #0d0d0d;
    border-radius: 12px;
    padding: 18px;
    margin-bottom: 12px;
    border: 1px solid #1a1a1a;
    cursor: pointer;
}

.card-info {
    font-weight: bold;
    font-size: 1.1rem;
    margin-bottom: 12px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.pair-name { color: #ffffff; }

.live-price {
    color: #ff8c00;
    font-family: monospace;
    font-size: 1rem;
}

.verdict {
    padding: 12px;
    border-radius: 8px;
    text-align: center;
    font-weight: 800;
    font-size: 1rem;
    margin-bottom: 12px;
}

.verdict.buy { background: #0ecb81; color: #000; }
.verdict.out { background: #1a0a0a; color: #f6465d; border: 1px solid #3b1015; }

.bos-badge {
    display: inline-block;
    font-size: 0.78rem;
    font-weight: 700;
    padding: 4px 10px;
    border-radius: 6px;
    margin-bottom: 8px;
}

.bos-bull { background: #0ecb8122; color: #0ecb81; border: 1px solid #0ecb8144; }
.bos-bear { background: #f6465d22; color: #f6465d; border: 1px solid #f6465d44; }

.sl-tp-row {
    display: flex;
    justify-content: space-between;
    font-size: 0.82rem;
    margin-top: 4px;
}

.sl-val { color: #f6465d; font-weight: 600; }
.tp-val { color: #0ecb81; font-weight: 600; }

.portfolio-card {
    background: #0d0d0d;
    padding: 40px 20px;
    border-radius: 16px;
    text-align: center;
    border: 1px solid #ff8c00;
}

.cap-val { font-size: 3rem; font-weight: 800; margin: 15px 0; }
.perf-val { font-size: 1.4rem; font-weight: 700; }
.perf-val.plus { color: #0ecb81; }
.perf-val.minus { color: #f6465d; }

.warning-text {
    font-size: 0.70rem;
    color: #b35f00;
    text-align: center;
    margin-top: 40px;
    padding: 10px;
    border-top: 1px solid #1a1a1a;
    line-height: 1.5;
}
